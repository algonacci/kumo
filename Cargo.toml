[package]
name = "kumo"
version = "0.1.0"
edition = "2024"
description = "A minimal, skills-based personal agent gateway"
license = "MIT"
repository = "https://github.com/algonacci/kumo"

[dependencies]
